void matchPairs(char nuts[], char bolts[], int n) {
	    // code here
	     sort(nuts,nuts+n);
	     sort(bolts,bolts+n);

	     // ! # $ % & * @ ^ ~  ascii values of these symbols are in increasing order so we just sort them

	}
